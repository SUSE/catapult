#!/usr/bin/env bash

# Dependencies options
######################

# Default versions in case the are undefined in the backend:
KUBECTL_VERSION="${KUBECTL_VERSION:-v1.17.0}"
HELM_VERSION="${HELM_VERSION:-v3.3.3}"
